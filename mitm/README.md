# MITM

Track hostnames needed by enabled rewrite scripts.

Only add hostnames required by active rewrites. Avoid broad wildcards unless the rewrite is trusted and necessary.

Example:

```text
hostname = example.com, api.example.com
```

